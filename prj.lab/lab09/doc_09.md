# Введение
Цель данного задания заключается в реализации алгоритма "Gray World" для автоматической балансировки белого цвета на изображении. Алгоритм основан на предположении, что средние значения интенсивностей каналов RGB должны быть равны, что соответствует нейтрально-серому цвету. Этот метод корректирует цвета изображения так, чтобы его общий тон стал более нейтральным и естественным.

# Основные этапы
1. Входное изображение загружается с использованием функции imread.
2. После успешной загрузки изображения вычисляются средние значения для каждого цветового канала (R, G, B) с помощью функции mean. Это позволяет определить, насколько каждый канал отклоняется от среднего значения, которое должно быть достигнуто для получения нейтрального серого цвета.
3. На основе вычисленных средних значений для каждого канала рассчитываются коэффициенты коррекции. Среднее значение всех каналов (avgGray) используется для вычисления коэффициентов для каждого канала:
- kB для синего канала,
- kG для зеленого канала,
- kR для красного канала.
Эти коэффициенты позволяют масштабировать значения каждого канала так, чтобы их средние значения стали равными.
4. Затем осуществляется коррекция значений пикселей каждого канала изображения. Для этого каждый пиксель умножается на соответствующий коэффициент коррекции. Для приведения результатов в допустимый диапазон значений (0-255) используется функция saturate_cast.
5. После применения алгоритма откорректированное изображение отображается с использованием функций imshow. Оригинальное и сбалансированное изображения показываются в отдельных окнах для визуального сравнения. Также результат сохраняется на диск с помощью функции imwrite.

# Описание функции оценки качества
Оценка качества изображения после применения метода серого мира выполняется с помощью двух метрик: индекса цветности (Colorfulness Index) и среднеквадратичной ошибки (MSE). Эти метрики помогают понять, насколько успешно выполнено цветовое балансирование.

Индекс цветности используется для оценки визуальной насыщенности и цветности изображения. Он основан на преобразовании изображения в цветовое пространство Lab, где компоненты L, a и b представляют яркость, красно-зеленую и желто-синюю цветовые компоненты соответственно.

Преобразование в цветовое пространство Lab:

Изображение преобразуется из цветового пространства BGR (Blue, Green, Red) в цветовое пространство Lab. Это делается с помощью функции cvtColor.
Вычисление среднего и стандартного отклонения:

Для каждого канала Lab (L, a, b) вычисляются среднее значение и стандартное отклонение с помощью функции meanStdDev.
Вычисление индекса цветности:

Индекс цветности рассчитывается как сумма двух величин: корня квадратного из суммы квадратов стандартных отклонений каналов a и b, и произведения 0.3 на корень квадратный из суммы квадратов средних значений каналов a и b.

Формула выглядит следующим образом:​

$$ Colorfulness = \sqrt{sigma_{ab}^2} + 0.3 * \sqrt{mue_{ab}^2} $$



sigma  — стандартное отклонение каналов a и b,

mue  — среднее значение каналов a и b.
  
Среднеквадратичная ошибка (MSE)
MSE используется для оценки разницы между двумя изображениями. Она вычисляет среднеквадратичное отклонение между соответствующими пикселями двух изображений.

Сначала вычисляется абсолютная разница между соответствующими пикселями исходного и сбалансированного изображений с помощью функции absdiff.

Результирующее изображение конвертируется в тип CV_32F для точных вычислений.

Формула выглядит следующим образом:

$$ \frac{1}{nmc} * \displaystyle\sum_{i=0}^{n-1}\displaystyle\sum_{j=0}^{m-1}\displaystyle\sum_{k=0}^{c-1} (I_1(i,j,k) - I_2(i,j,k))^2    $$


​
n, 𝑚 - размеры изображения,

c — количество цветовых каналов,

I1, I2  — исходное и сбалансированное изображения соответственно.

Функция measureQuality
Эта функция вычисляет обе метрики и выводит результаты:

Вычисление индекса цветности для исходного и сбалансированного изображений:

Используется функция calculateColorfulnessIndex.
Вычисление MSE между исходным и сбалансированным изображениями:

Используется функция calculateMSE.

Результаты индексов цветности и MSE выводятся на экран для оценки изменений.

# Результат

<picture>
  <img src="../../pictures/lab03_test1.jpg" width="960" height="612">
</picture>
   
Рис. 1 – тестовое изображение 1

<picture>
  <img src="../../pictures/lab03_test2.jpg" width="550" height="400">
</picture>
   
Рис. 2 – тестовое изображение 2

<picture>
  <img src="../../pictures/lab09_gray_world_balanced1.jpg" width="960" height="612">
</picture>
   
Рис. 3 – откорректированное изображение 1

<picture>
  <img src="../../pictures/lab09_gray_world_balanced.jpg" width="550" height="400">
</picture>
   
Рис. 4 – откорректированное изображение 2

# Таблица по оценке качества

|Number of image|Cf index for test image|Cf index for balanced image|MSE|
|-|-|--------|---|
|1|77|70|250|
|2|68|68|110|

Снижение индекса цветности у первого изображения указывает на уменьшение насыщенности цвета после применения алгоритма Gray World. Это может означать, что алгоритм сбалансировал цвета, уменьшив их избыточную насыщенность, что может быть полезным в случае перенасыщенных изображений. Высокое значение MSE указывает на значительные различия между тестовым и сбалансированным изображениями. Это подтверждает, что алгоритм Gray World существенно изменил пиксели изображения для достижения цветового баланса.

Никакого изменения индекса цветности у второго изображения не произошло. Это свидетельствует о том, что исходное изображение уже имело сбалансированные цвета, и алгоритм Gray World не внес заметных изменений. Более низкое значение MSE по сравнению с изображением 1 указывает на меньшие изменения между тестовым и сбалансированным изображениями. Это совпадает с наблюдением, что индекс цветности остался неизменным, что указывает на меньшую необходимость корректировки цветов в изображении 2.

# Сравнение с другими алгоритмами

1. Алгоритм Белого Баланса (White Balance Algorithm)
Этот алгоритм корректирует цвета изображения, чтобы нейтральные цвета (серые или белые) отображались как нейтральные. Это достигается путем компенсации цветового оттенка, вызванного различными источниками освещения. Белый баланс часто используется в фотографиях для коррекции цветовых искажений, вызванных различными типами освещения (солнечный свет, лампы накаливания, флуоресцентное освещение и т.д.).

2. Алгоритм Perfect Reflector (Perfect Reflector Algorithm)
Этот метод предполагает, что в сцене есть объект, который является идеальным белым отражателем. Исходя из этого предположения, алгоритм корректирует цвета таким образом, чтобы этот объект выглядел белым на изображении. Это может быть полезно, если на изображении действительно присутствует белый или серый объект.

3. Алгоритм Retinex
Алгоритм Retinex имитирует способ, которым человеческий глаз и мозг воспринимают цвета при различных условиях освещения. Этот алгоритм учитывает как локальную, так и глобальную информацию о яркости и цвете, чтобы улучшить восприятие цвета. Существует несколько вариантов этого алгоритма, включая Single Scale Retinex (SSR) и Multi-Scale Retinex (MSR).

4. Алгоритм Histogram Equalization (Гистограммовое выравнивание)
Этот метод улучшает контраст изображения путем перераспределения значений интенсивности, чтобы гистограмма интенсивности была более равномерно распределена. Хотя этот алгоритм не нацелен специально на цветовой баланс, он может улучшить видимость деталей в изображении.

5. Алгоритм Color Balance Using Color Transfer (Перенос цвета)
Этот метод включает в себя перенос цветовых характеристик эталонного изображения на тестовое изображение. Это полезно для достижения визуально приятного цветового баланса, который соответствует эталонному изображению.

6. Алгоритм Shades of Gray
Этот метод является расширением алгоритма Gray World и основывается на среднем значении не нейтральных цветов изображения. Он вычисляет среднее значение каждого цветового канала и корректирует их так, чтобы они все были одинаковыми, как и в Gray World, но с учетом силы каждого цветового канала.

Сравнение:
- Gray World: Простой и быстрый алгоритм, который предполагает, что средний цвет всего изображения должен быть нейтрально серым.
- Белый Баланс: Корректирует цвета в зависимости от типа освещения, что более точно для различных условий освещения.
- Perfect Reflector: Требует наличия идеального белого объекта в сцене, что не всегда возможно.
- Retinex: Более сложный, но предоставляет более естественные результаты, имитируя человеческое восприятие.
- Histogram Equalization: Улучшает контраст, но не нацелен на корректировку цветового баланса.
- Color Transfer: Использует эталонное изображение для достижения визуально приятных цветов.
- Shades of Gray: Улучшает Gray World, учитывая силы каждого цветового канала.

Алгоритм Gray World наиболее удобен для быстрого улучшения цветового баланса, но для более точных и специфичных корректировок могут быть предпочтительны другие алгоритмы, такие как белый баланс или Retinex.

# Вывод
Алгоритм Gray World показал различные результаты в зависимости от исходных характеристик изображений. В случае изображения 1, алгоритм значительно снизил насыщенность цветов и внес значительные изменения в пиксельные значения, что привело к высокому значению MSE. Это может быть полезно для изображений с перенасыщенными цветами. В случае изображения 2, алгоритм не внес заметных изменений, что свидетельствует о его способности сохранять уже сбалансированные изображения.

